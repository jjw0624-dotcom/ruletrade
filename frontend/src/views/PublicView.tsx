import { STRATEGY_EXAMPLES, type ExampleId } from "../domain/examples";
import { StartingPointCard } from "../components/StartingPointCard";

export function PublicView({ onExample, onHome, onCreate }: { onExample: (id: ExampleId) => void; onHome: () => void; onCreate: () => void }) {
  return <section className="public-page"><header className="public-nav"><button className="brand"><span className="brand-mark">R</span><span>RuleTrade</span></button><div><button className="text-button" onClick={onHome}>My strategies</button><button className="primary-button" onClick={onCreate}>Build a strategy</button></div></header><main><section className="public-hero"><span className="eyebrow">Investment research, explained</span><h1>Test your investment ideas.</h1><p>Build a strategy, see how it behaved, and understand why.</p><button className="primary-button" onClick={onCreate}>Build a strategy</button></section><section className="public-questions"><header><span className="eyebrow">Things you might have wondered</span><h2>Start with a question</h2></header><div>{STRATEGY_EXAMPLES.map((point) => <StartingPointCard key={point.id} point={point} onSelect={onExample} />)}</div></section><section className="public-close"><h2>Want to build your own?</h2><button className="primary-button" onClick={onCreate}>Build a strategy</button><p>Build → Test → Understand → Change → Compare</p></section></main></section>;
}
