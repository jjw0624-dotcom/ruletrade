import { STRATEGY_EXAMPLES, type ExampleId } from "../domain/examples";
export function ExploreView({ onOpen, onUse, creating }: { onOpen: (id: ExampleId) => void; onUse: (id: ExampleId) => void; creating?: ExampleId | null }) {
  return <section className="page explore-page"><header className="page-heading"><span className="eyebrow">Explore</span><h1>Start with a strategy you can understand</h1><p>Open a backend-supported example, inspect every rule, and run it through the real backtest engine.</p></header><div className="example-grid">
    {STRATEGY_EXAMPLES.map((example, index) => <article className="example-card" key={example.id}><div className="example-number">0{index + 1}</div><div><h2>{example.title}</h2><p>{example.description}</p></div><p className="example-detail">{example.detail}</p><div className="tag-row">{example.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><div className="card-actions"><button className="secondary-button" onClick={() => onOpen(example.id)}>Preview</button><button className="primary-button" disabled={creating === example.id} onClick={() => onUse(example.id)}>{creating === example.id ? "Opening…" : "Use this strategy"}</button></div></article>)}
  </div></section>;
}
